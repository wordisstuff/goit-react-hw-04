const SearchBar = () => {
  return (
    <header>
      <form>
        <input type="text" placeholder="Search..." />
        <button type="submit">🍭</button>
      </form>
    </header>
  );
};

export default SearchBar;
